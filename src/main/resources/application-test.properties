# Test Profile - Supabase Connection Test
spring.application.name=dream-shops

# Supabase PostgreSQL Database Configuration for Testing (Session Mode)
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.jknhsdrxebyixhjklfxh
spring.datasource.password=3t5d5d0StNHQY9Il

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# API Configuration
api.prefix=/api/v1

# JWT Configuration
jwt.secret=mySecretKey
jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server Configuration
server.port=8080

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000

# Redis Configuration (Optional - disabled for test)
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=
spring.redis.timeout=2000ms

# Logging Configuration
logging.level.root=INFO
logging.level.com.seyman.dreamshops=DEBUG
logging.level.org.springframework=INFO
logging.level.org.hibernate=INFO

# Management endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always 